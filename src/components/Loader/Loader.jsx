import style from "./Loader.module.scss";

export const Loader = () => {
  return <span className={style.loader}></span>;
};

export const Spiner = () => {
  return <span className={style.spiner}></span>;
};
