// Ingredients
export const selectIngredients = (state) => state.preparation.ingredients;
export const selectCategories = (state) => state.preparation.categories;
export const selectGlasses = (state) => state.preparation.glasses;

export const selectPopularRecipes = (state) => state.preparation.popular;

export const selectPreparation = (state) => state.preparation;
