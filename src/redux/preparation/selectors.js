// Ingredients
export const selectIngredients = (state) => state.preparation.ingredients;
export const selectCategories = (state) => state.preparation.ingredients;
export const selectGlasses = (state) => state.preparation.ingredients;

export const selectPreparation = (state) => state.preparation;
